name: image-build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
      - uses: actions/checkout@v4
      - name: install
        run: |
          brew install docker
          colima start
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3

